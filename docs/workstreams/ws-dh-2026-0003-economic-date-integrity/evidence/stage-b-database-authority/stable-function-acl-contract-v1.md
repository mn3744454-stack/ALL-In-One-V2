# Stable Function-ACL Contract v1 — Corrected Option B

RM-DH-004 / Phase 1 — Economic Date Integrity / WS-DH-2026-0003 — Stage B Database Authority

## 1. Identity and Binding

| Field | Value |
| --- | --- |
| Project | Dayli Horse / ديلي هورس |
| Roadmap | RM-DH-004 — Financial Truth Stabilization & Historical Data Migration |
| Phase | Phase 1 — Economic Date Integrity |
| Workstream | WS-DH-2026-0003 — Economic Date Integrity |
| Related deferred Workstream | WS-DH-2026-0005 — POS Financial Isolation |
| Prompt lineage | Shared Tenant Operational Finance + Historical Migration |
| Prompt ID | PROMPT-DH-SHARED-OPERATIONAL-FINANCE-HISTORICAL-MIGRATION-ECONOMIC-DATE-STAGE-B-CORRECTED-OPTION-B-LIVE-CANONICAL-STATE-A-STABLE-CONTRACT-AND-COMPLETE-ROLLBACK-REISSUE-28 |
| Prompt preparation | 04-08-2026 15:12 Asia/Riyadh (UTC+03:00) |
| Execution evidence time | 04-08-2026 15:21 Asia/Riyadh (UTC+03:00) — 2026-08-04T12:21Z |
| Database / project | postgres (project ref vhxglsvxwwpmoqjabfmj) |
| PostgreSQL version | 17.6 |
| Read role | supabase_read_only_user |
| Branch | edit/edt-10f270a2-2c44-465e-834b-897e55af825c |
| HEAD before write | ca71a1ccca02ce0ec99ed6914b84eb2073c28a4e |
| Forward migration | `supabase/migrations/20260804083738_3d2d0ddf-5f5f-42f8-9bd0-832bc4430b78.sql` |
| Forward version | 20260804083738 |
| Forward SHA-256 | `602804b939876ee3a2a19114296de3ff080aafc574a3b0913bed1fa6e80baa5f` |
| Active Rollback artifact | `docs/workstreams/ws-dh-2026-0003-economic-date-integrity/evidence/stage-b-database-authority/rollback.sql` |
| Previous Rollback SHA-256 | `62be2f2773dddc0b8e1aab33aaef4b70c9ba304df5cc8879f616e32c74460baa` |

### Owner approval

The Owner was presented with the corrected Option-B contract (live schema-qualified Function
identity, live Boolean rendering `f`, corrected State-A candidate 37 rows / 5498 bytes /
MD5 `36da554aef9a68d2acfbe9e1663c5def`, full SHA-256 computed before persistence,
repository-only writes) and replied:

`اعتمد. يلا نبدأ`

This is the explicit Owner approval bound to this artifact.

## 2. Approved Option B

- The stable blocking grantee scope is exactly: PUBLIC, anon, authenticated, service_role, postgres.
- Every role named `sandbox_exec` or beginning with `sandbox_exec_` is platform-managed
  infrastructure and is **observational only**.
- Platform sandbox observation drift is **non-blocking by itself**.
- Governed role drift (PUBLIC, anon, authenticated, service_role, postgres) remains **blocking**.
- No live database rebaseline occurred in this run.
- No database correction, privilege change, migration or rollback execution occurred in this run.

## 3. Complete Canonical Algorithm

### 3.1 Exact fourteen-Function set

1. `public._finance_ledger_insert(uuid,uuid,text,text,uuid,numeric,date,text,text,uuid,jsonb,uuid)`
2. `public._finance_invoice_approve_inline(uuid,uuid,uuid)`
3. `public.create_invoice_with_items(uuid,uuid,jsonb)`
4. `public.update_invoice_with_items(uuid,uuid,uuid,jsonb)`
5. `public.delete_draft_invoice(uuid,uuid,uuid)`
6. `public.approve_invoice(uuid,uuid,uuid)`
7. `public.cancel_invoice(uuid,uuid,uuid,date,text)`
8. `public.post_payment(uuid,uuid,uuid,numeric,date,text,uuid,jsonb)`
9. `public.post_payment_session(uuid,uuid,jsonb)`
10. `public.post_invoice_payments(uuid,uuid,uuid,uuid,date,jsonb)`
11. `public.post_expense_with_ledger(uuid,uuid,uuid)`
12. `public.post_manual_ledger_adjustment(uuid,uuid,uuid,numeric,date,text)`
13. `public.create_source_checkout_invoice(uuid,uuid,jsonb)`
14. `public.create_pos_sale(uuid,uuid,jsonb)`

The short signatures above are **resolution keys only**. Every canonical line uses the live
identity generated by the expression in §3.2.

### 3.2 Function identity expression

```sql
format(
  '%I.%I(%s)',
  n.nspname,
  p.proname,
  pg_get_function_identity_arguments(p.oid)
)
```

No Function identity may be hand-typed, shortened or normalized.

### 3.3 Canonical line

```text
schema_qualified_function_identity|grantor_role_name|grantee_role_name|privilege_type|is_grantable
```

- Grantor rendering: exact `pg_roles.rolname`; unresolved role `<NULL>`.
- Grantee rendering: ACL grantee OID `0` renders as `PUBLIC`; otherwise exact
  `pg_roles.rolname`; unresolved role `<NULL>`.
- Boolean rendering: live text rendering `f` (non-grantable) and `t` (grantable).
  The literals `false`, `true`, `0` and `1` are forbidden.
- Field separator: `|`; line separator: `E'\n'`.
- Ordering: ascending complete canonical line text.
- Encoding: UTF-8. Terminal newline: none.
- No role OID appears inside the canonical string.
- MD5 = `md5(canonical_string)`.
- SHA-256 = `encode(sha256(convert_to(canonical_string,'UTF8')),'hex')`.

### 3.4 Stable grantee filter

A row enters the Stable Contract only when its grantee is PUBLIC, anon, authenticated,
service_role or postgres. The grantor is not filtered.

### 3.5 Synthetic State-A reconstruction

State A is reconstructed read-only from the live State-B stable rows plus exactly two synthetic
rows, both using the live `create_pos_sale` identity and Boolean `f`:

```text
public.create_pos_sale(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)|postgres|anon|EXECUTE|f
public.create_pos_sale(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)|postgres|authenticated|EXECUTE|f
```

No live ACL was altered to produce State A.

## 4. Complete State-B Stable Contract (live)

Artifact name: `OPTION-B STATE-B STABLE FUNCTION-ACL CONTRACT`

| Field | Value |
| --- | --- |
| Rows | 35 |
| UTF-8 bytes | 5277 |
| MD5 | `31a3c711f72f419e75f89a234a9923cc` |
| SHA-256 | `67128e3269272e695b4452247eed409378b5f30d10c5df54a9d6b617abeea404` |

Complete canonical string (all 35 lines, no ellipsis, no terminal newline):

```text
public._finance_invoice_approve_inline(p_tenant_id uuid, p_invoice_id uuid, p_actor uuid)|postgres|postgres|EXECUTE|f
public._finance_invoice_approve_inline(p_tenant_id uuid, p_invoice_id uuid, p_actor uuid)|postgres|service_role|EXECUTE|f
public._finance_ledger_insert(p_tenant_id uuid, p_client_id uuid, p_entry_type text, p_reference_type text, p_reference_id uuid, p_amount numeric, p_effective_date date, p_description text, p_payment_method text, p_payment_session_id uuid, p_metadata jsonb, p_created_by uuid)|postgres|postgres|EXECUTE|f
public._finance_ledger_insert(p_tenant_id uuid, p_client_id uuid, p_entry_type text, p_reference_type text, p_reference_id uuid, p_amount numeric, p_effective_date date, p_description text, p_payment_method text, p_payment_session_id uuid, p_metadata jsonb, p_created_by uuid)|postgres|service_role|EXECUTE|f
public.approve_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid)|postgres|authenticated|EXECUTE|f
public.approve_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid)|postgres|postgres|EXECUTE|f
public.approve_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid)|postgres|service_role|EXECUTE|f
public.cancel_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid, p_effective_date date, p_reason text)|postgres|authenticated|EXECUTE|f
public.cancel_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid, p_effective_date date, p_reason text)|postgres|postgres|EXECUTE|f
public.create_invoice_with_items(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)|postgres|authenticated|EXECUTE|f
public.create_invoice_with_items(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)|postgres|postgres|EXECUTE|f
public.create_pos_sale(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)|postgres|postgres|EXECUTE|f
public.create_pos_sale(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)|postgres|service_role|EXECUTE|f
public.create_source_checkout_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)|postgres|authenticated|EXECUTE|f
public.create_source_checkout_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)|postgres|postgres|EXECUTE|f
public.create_source_checkout_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)|postgres|service_role|EXECUTE|f
public.delete_draft_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid)|postgres|authenticated|EXECUTE|f
public.delete_draft_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid)|postgres|postgres|EXECUTE|f
public.delete_draft_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid)|postgres|service_role|EXECUTE|f
public.post_expense_with_ledger(p_tenant_id uuid, p_idempotency_key uuid, p_expense_id uuid)|postgres|authenticated|EXECUTE|f
public.post_expense_with_ledger(p_tenant_id uuid, p_idempotency_key uuid, p_expense_id uuid)|postgres|postgres|EXECUTE|f
public.post_expense_with_ledger(p_tenant_id uuid, p_idempotency_key uuid, p_expense_id uuid)|postgres|service_role|EXECUTE|f
public.post_invoice_payments(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid, p_account_id uuid, p_payment_date date, p_payments jsonb)|postgres|authenticated|EXECUTE|f
public.post_invoice_payments(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid, p_account_id uuid, p_payment_date date, p_payments jsonb)|postgres|postgres|EXECUTE|f
public.post_manual_ledger_adjustment(p_tenant_id uuid, p_idempotency_key uuid, p_client_id uuid, p_amount numeric, p_effective_date date, p_description text)|postgres|authenticated|EXECUTE|f
public.post_manual_ledger_adjustment(p_tenant_id uuid, p_idempotency_key uuid, p_client_id uuid, p_amount numeric, p_effective_date date, p_description text)|postgres|postgres|EXECUTE|f
public.post_manual_ledger_adjustment(p_tenant_id uuid, p_idempotency_key uuid, p_client_id uuid, p_amount numeric, p_effective_date date, p_description text)|postgres|service_role|EXECUTE|f
public.post_payment(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid, p_amount numeric, p_payment_date date, p_payment_method text, p_account_id uuid, p_payload jsonb)|postgres|authenticated|EXECUTE|f
public.post_payment(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid, p_amount numeric, p_payment_date date, p_payment_method text, p_account_id uuid, p_payload jsonb)|postgres|postgres|EXECUTE|f
public.post_payment(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid, p_amount numeric, p_payment_date date, p_payment_method text, p_account_id uuid, p_payload jsonb)|postgres|service_role|EXECUTE|f
public.post_payment_session(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)|postgres|authenticated|EXECUTE|f
public.post_payment_session(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)|postgres|postgres|EXECUTE|f
public.post_payment_session(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)|postgres|service_role|EXECUTE|f
public.update_invoice_with_items(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid, p_payload jsonb)|postgres|authenticated|EXECUTE|f
public.update_invoice_with_items(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid, p_payload jsonb)|postgres|postgres|EXECUTE|f
```

Role/function matrix:

| # | Function identity | Grantor | Grantee | Privilege | Grantable |
| ---: | --- | --- | --- | --- | --- |
| 1 | `public._finance_invoice_approve_inline(p_tenant_id uuid, p_invoice_id uuid, p_actor uuid)` | postgres | postgres | EXECUTE | f |
| 2 | `public._finance_invoice_approve_inline(p_tenant_id uuid, p_invoice_id uuid, p_actor uuid)` | postgres | service_role | EXECUTE | f |
| 3 | `public._finance_ledger_insert(p_tenant_id uuid, p_client_id uuid, p_entry_type text, p_reference_type text, p_reference_id uuid, p_amount numeric, p_effective_date date, p_description text, p_payment_method text, p_payment_session_id uuid, p_metadata jsonb, p_created_by uuid)` | postgres | postgres | EXECUTE | f |
| 4 | `public._finance_ledger_insert(p_tenant_id uuid, p_client_id uuid, p_entry_type text, p_reference_type text, p_reference_id uuid, p_amount numeric, p_effective_date date, p_description text, p_payment_method text, p_payment_session_id uuid, p_metadata jsonb, p_created_by uuid)` | postgres | service_role | EXECUTE | f |
| 5 | `public.approve_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid)` | postgres | authenticated | EXECUTE | f |
| 6 | `public.approve_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid)` | postgres | postgres | EXECUTE | f |
| 7 | `public.approve_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid)` | postgres | service_role | EXECUTE | f |
| 8 | `public.cancel_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid, p_effective_date date, p_reason text)` | postgres | authenticated | EXECUTE | f |
| 9 | `public.cancel_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid, p_effective_date date, p_reason text)` | postgres | postgres | EXECUTE | f |
| 10 | `public.create_invoice_with_items(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)` | postgres | authenticated | EXECUTE | f |
| 11 | `public.create_invoice_with_items(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)` | postgres | postgres | EXECUTE | f |
| 12 | `public.create_pos_sale(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)` | postgres | postgres | EXECUTE | f |
| 13 | `public.create_pos_sale(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)` | postgres | service_role | EXECUTE | f |
| 14 | `public.create_source_checkout_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)` | postgres | authenticated | EXECUTE | f |
| 15 | `public.create_source_checkout_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)` | postgres | postgres | EXECUTE | f |
| 16 | `public.create_source_checkout_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)` | postgres | service_role | EXECUTE | f |
| 17 | `public.delete_draft_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid)` | postgres | authenticated | EXECUTE | f |
| 18 | `public.delete_draft_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid)` | postgres | postgres | EXECUTE | f |
| 19 | `public.delete_draft_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid)` | postgres | service_role | EXECUTE | f |
| 20 | `public.post_expense_with_ledger(p_tenant_id uuid, p_idempotency_key uuid, p_expense_id uuid)` | postgres | authenticated | EXECUTE | f |
| 21 | `public.post_expense_with_ledger(p_tenant_id uuid, p_idempotency_key uuid, p_expense_id uuid)` | postgres | postgres | EXECUTE | f |
| 22 | `public.post_expense_with_ledger(p_tenant_id uuid, p_idempotency_key uuid, p_expense_id uuid)` | postgres | service_role | EXECUTE | f |
| 23 | `public.post_invoice_payments(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid, p_account_id uuid, p_payment_date date, p_payments jsonb)` | postgres | authenticated | EXECUTE | f |
| 24 | `public.post_invoice_payments(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid, p_account_id uuid, p_payment_date date, p_payments jsonb)` | postgres | postgres | EXECUTE | f |
| 25 | `public.post_manual_ledger_adjustment(p_tenant_id uuid, p_idempotency_key uuid, p_client_id uuid, p_amount numeric, p_effective_date date, p_description text)` | postgres | authenticated | EXECUTE | f |
| 26 | `public.post_manual_ledger_adjustment(p_tenant_id uuid, p_idempotency_key uuid, p_client_id uuid, p_amount numeric, p_effective_date date, p_description text)` | postgres | postgres | EXECUTE | f |
| 27 | `public.post_manual_ledger_adjustment(p_tenant_id uuid, p_idempotency_key uuid, p_client_id uuid, p_amount numeric, p_effective_date date, p_description text)` | postgres | service_role | EXECUTE | f |
| 28 | `public.post_payment(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid, p_amount numeric, p_payment_date date, p_payment_method text, p_account_id uuid, p_payload jsonb)` | postgres | authenticated | EXECUTE | f |
| 29 | `public.post_payment(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid, p_amount numeric, p_payment_date date, p_payment_method text, p_account_id uuid, p_payload jsonb)` | postgres | postgres | EXECUTE | f |
| 30 | `public.post_payment(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid, p_amount numeric, p_payment_date date, p_payment_method text, p_account_id uuid, p_payload jsonb)` | postgres | service_role | EXECUTE | f |
| 31 | `public.post_payment_session(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)` | postgres | authenticated | EXECUTE | f |
| 32 | `public.post_payment_session(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)` | postgres | postgres | EXECUTE | f |
| 33 | `public.post_payment_session(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)` | postgres | service_role | EXECUTE | f |
| 34 | `public.update_invoice_with_items(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid, p_payload jsonb)` | postgres | authenticated | EXECUTE | f |
| 35 | `public.update_invoice_with_items(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid, p_payload jsonb)` | postgres | postgres | EXECUTE | f |

## 5. Complete Corrected State-A Stable Contract (synthetic, read-only)

Artifact name: `OPTION-B STATE-A STABLE FUNCTION-ACL CONTRACT (CORRECTED)`

| Field | Value |
| --- | --- |
| Rows | 37 |
| UTF-8 bytes | 5498 |
| MD5 | `36da554aef9a68d2acfbe9e1663c5def` |
| SHA-256 | `5a7c4fa94cc44fc330503b58a13dde678a83206270e79f681a295988cdd63db2` |

The SHA-256 above was calculated in this run from the exact corrected canonical string and is
the authoritative Option-B State-A SHA-256. Binding conditions satisfied: 37 rows; 5498 bytes;
MD5 `36da554aef9a68d2acfbe9e1663c5def`; both synthetic lines use the live schema-qualified identity; both end in
`|EXECUTE|f`; no additional row; no terminal newline.

Complete canonical string (all 37 lines, no ellipsis, no terminal newline):

```text
public._finance_invoice_approve_inline(p_tenant_id uuid, p_invoice_id uuid, p_actor uuid)|postgres|postgres|EXECUTE|f
public._finance_invoice_approve_inline(p_tenant_id uuid, p_invoice_id uuid, p_actor uuid)|postgres|service_role|EXECUTE|f
public._finance_ledger_insert(p_tenant_id uuid, p_client_id uuid, p_entry_type text, p_reference_type text, p_reference_id uuid, p_amount numeric, p_effective_date date, p_description text, p_payment_method text, p_payment_session_id uuid, p_metadata jsonb, p_created_by uuid)|postgres|postgres|EXECUTE|f
public._finance_ledger_insert(p_tenant_id uuid, p_client_id uuid, p_entry_type text, p_reference_type text, p_reference_id uuid, p_amount numeric, p_effective_date date, p_description text, p_payment_method text, p_payment_session_id uuid, p_metadata jsonb, p_created_by uuid)|postgres|service_role|EXECUTE|f
public.approve_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid)|postgres|authenticated|EXECUTE|f
public.approve_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid)|postgres|postgres|EXECUTE|f
public.approve_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid)|postgres|service_role|EXECUTE|f
public.cancel_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid, p_effective_date date, p_reason text)|postgres|authenticated|EXECUTE|f
public.cancel_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid, p_effective_date date, p_reason text)|postgres|postgres|EXECUTE|f
public.create_invoice_with_items(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)|postgres|authenticated|EXECUTE|f
public.create_invoice_with_items(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)|postgres|postgres|EXECUTE|f
public.create_pos_sale(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)|postgres|anon|EXECUTE|f
public.create_pos_sale(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)|postgres|authenticated|EXECUTE|f
public.create_pos_sale(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)|postgres|postgres|EXECUTE|f
public.create_pos_sale(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)|postgres|service_role|EXECUTE|f
public.create_source_checkout_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)|postgres|authenticated|EXECUTE|f
public.create_source_checkout_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)|postgres|postgres|EXECUTE|f
public.create_source_checkout_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)|postgres|service_role|EXECUTE|f
public.delete_draft_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid)|postgres|authenticated|EXECUTE|f
public.delete_draft_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid)|postgres|postgres|EXECUTE|f
public.delete_draft_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid)|postgres|service_role|EXECUTE|f
public.post_expense_with_ledger(p_tenant_id uuid, p_idempotency_key uuid, p_expense_id uuid)|postgres|authenticated|EXECUTE|f
public.post_expense_with_ledger(p_tenant_id uuid, p_idempotency_key uuid, p_expense_id uuid)|postgres|postgres|EXECUTE|f
public.post_expense_with_ledger(p_tenant_id uuid, p_idempotency_key uuid, p_expense_id uuid)|postgres|service_role|EXECUTE|f
public.post_invoice_payments(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid, p_account_id uuid, p_payment_date date, p_payments jsonb)|postgres|authenticated|EXECUTE|f
public.post_invoice_payments(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid, p_account_id uuid, p_payment_date date, p_payments jsonb)|postgres|postgres|EXECUTE|f
public.post_manual_ledger_adjustment(p_tenant_id uuid, p_idempotency_key uuid, p_client_id uuid, p_amount numeric, p_effective_date date, p_description text)|postgres|authenticated|EXECUTE|f
public.post_manual_ledger_adjustment(p_tenant_id uuid, p_idempotency_key uuid, p_client_id uuid, p_amount numeric, p_effective_date date, p_description text)|postgres|postgres|EXECUTE|f
public.post_manual_ledger_adjustment(p_tenant_id uuid, p_idempotency_key uuid, p_client_id uuid, p_amount numeric, p_effective_date date, p_description text)|postgres|service_role|EXECUTE|f
public.post_payment(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid, p_amount numeric, p_payment_date date, p_payment_method text, p_account_id uuid, p_payload jsonb)|postgres|authenticated|EXECUTE|f
public.post_payment(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid, p_amount numeric, p_payment_date date, p_payment_method text, p_account_id uuid, p_payload jsonb)|postgres|postgres|EXECUTE|f
public.post_payment(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid, p_amount numeric, p_payment_date date, p_payment_method text, p_account_id uuid, p_payload jsonb)|postgres|service_role|EXECUTE|f
public.post_payment_session(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)|postgres|authenticated|EXECUTE|f
public.post_payment_session(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)|postgres|postgres|EXECUTE|f
public.post_payment_session(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)|postgres|service_role|EXECUTE|f
public.update_invoice_with_items(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid, p_payload jsonb)|postgres|authenticated|EXECUTE|f
public.update_invoice_with_items(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid, p_payload jsonb)|postgres|postgres|EXECUTE|f
```

Exact two synthetic lines:

```text
public.create_pos_sale(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)|postgres|anon|EXECUTE|f
public.create_pos_sale(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)|postgres|authenticated|EXECUTE|f
```

Role/function matrix:

| # | Function identity | Grantor | Grantee | Privilege | Grantable |
| ---: | --- | --- | --- | --- | --- |
| 1 | `public._finance_invoice_approve_inline(p_tenant_id uuid, p_invoice_id uuid, p_actor uuid)` | postgres | postgres | EXECUTE | f |
| 2 | `public._finance_invoice_approve_inline(p_tenant_id uuid, p_invoice_id uuid, p_actor uuid)` | postgres | service_role | EXECUTE | f |
| 3 | `public._finance_ledger_insert(p_tenant_id uuid, p_client_id uuid, p_entry_type text, p_reference_type text, p_reference_id uuid, p_amount numeric, p_effective_date date, p_description text, p_payment_method text, p_payment_session_id uuid, p_metadata jsonb, p_created_by uuid)` | postgres | postgres | EXECUTE | f |
| 4 | `public._finance_ledger_insert(p_tenant_id uuid, p_client_id uuid, p_entry_type text, p_reference_type text, p_reference_id uuid, p_amount numeric, p_effective_date date, p_description text, p_payment_method text, p_payment_session_id uuid, p_metadata jsonb, p_created_by uuid)` | postgres | service_role | EXECUTE | f |
| 5 | `public.approve_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid)` | postgres | authenticated | EXECUTE | f |
| 6 | `public.approve_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid)` | postgres | postgres | EXECUTE | f |
| 7 | `public.approve_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid)` | postgres | service_role | EXECUTE | f |
| 8 | `public.cancel_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid, p_effective_date date, p_reason text)` | postgres | authenticated | EXECUTE | f |
| 9 | `public.cancel_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid, p_effective_date date, p_reason text)` | postgres | postgres | EXECUTE | f |
| 10 | `public.create_invoice_with_items(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)` | postgres | authenticated | EXECUTE | f |
| 11 | `public.create_invoice_with_items(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)` | postgres | postgres | EXECUTE | f |
| 12 | `public.create_pos_sale(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)` | postgres | anon | EXECUTE | f |
| 13 | `public.create_pos_sale(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)` | postgres | authenticated | EXECUTE | f |
| 14 | `public.create_pos_sale(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)` | postgres | postgres | EXECUTE | f |
| 15 | `public.create_pos_sale(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)` | postgres | service_role | EXECUTE | f |
| 16 | `public.create_source_checkout_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)` | postgres | authenticated | EXECUTE | f |
| 17 | `public.create_source_checkout_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)` | postgres | postgres | EXECUTE | f |
| 18 | `public.create_source_checkout_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)` | postgres | service_role | EXECUTE | f |
| 19 | `public.delete_draft_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid)` | postgres | authenticated | EXECUTE | f |
| 20 | `public.delete_draft_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid)` | postgres | postgres | EXECUTE | f |
| 21 | `public.delete_draft_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid)` | postgres | service_role | EXECUTE | f |
| 22 | `public.post_expense_with_ledger(p_tenant_id uuid, p_idempotency_key uuid, p_expense_id uuid)` | postgres | authenticated | EXECUTE | f |
| 23 | `public.post_expense_with_ledger(p_tenant_id uuid, p_idempotency_key uuid, p_expense_id uuid)` | postgres | postgres | EXECUTE | f |
| 24 | `public.post_expense_with_ledger(p_tenant_id uuid, p_idempotency_key uuid, p_expense_id uuid)` | postgres | service_role | EXECUTE | f |
| 25 | `public.post_invoice_payments(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid, p_account_id uuid, p_payment_date date, p_payments jsonb)` | postgres | authenticated | EXECUTE | f |
| 26 | `public.post_invoice_payments(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid, p_account_id uuid, p_payment_date date, p_payments jsonb)` | postgres | postgres | EXECUTE | f |
| 27 | `public.post_manual_ledger_adjustment(p_tenant_id uuid, p_idempotency_key uuid, p_client_id uuid, p_amount numeric, p_effective_date date, p_description text)` | postgres | authenticated | EXECUTE | f |
| 28 | `public.post_manual_ledger_adjustment(p_tenant_id uuid, p_idempotency_key uuid, p_client_id uuid, p_amount numeric, p_effective_date date, p_description text)` | postgres | postgres | EXECUTE | f |
| 29 | `public.post_manual_ledger_adjustment(p_tenant_id uuid, p_idempotency_key uuid, p_client_id uuid, p_amount numeric, p_effective_date date, p_description text)` | postgres | service_role | EXECUTE | f |
| 30 | `public.post_payment(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid, p_amount numeric, p_payment_date date, p_payment_method text, p_account_id uuid, p_payload jsonb)` | postgres | authenticated | EXECUTE | f |
| 31 | `public.post_payment(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid, p_amount numeric, p_payment_date date, p_payment_method text, p_account_id uuid, p_payload jsonb)` | postgres | postgres | EXECUTE | f |
| 32 | `public.post_payment(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid, p_amount numeric, p_payment_date date, p_payment_method text, p_account_id uuid, p_payload jsonb)` | postgres | service_role | EXECUTE | f |
| 33 | `public.post_payment_session(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)` | postgres | authenticated | EXECUTE | f |
| 34 | `public.post_payment_session(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)` | postgres | postgres | EXECUTE | f |
| 35 | `public.post_payment_session(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)` | postgres | service_role | EXECUTE | f |
| 36 | `public.update_invoice_with_items(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid, p_payload jsonb)` | postgres | authenticated | EXECUTE | f |
| 37 | `public.update_invoice_with_items(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid, p_payload jsonb)` | postgres | postgres | EXECUTE | f |

### 5.1 Proof of the only State-A / State-B delta

1. Both synthetic rows are absent from live State B — live `create_pos_sale` stable grants are
   `postgres` and `service_role` only.
2. Both rows existed in the accepted State-A role contract: the Forward migration
   `20260804083738` revoked `create_pos_sale` EXECUTE from anon and authenticated.
3. PUBLIC never held a POS EXECUTE grant in State A or State B.
4. No other stable grantee (PUBLIC, anon, authenticated, service_role, postgres) changed over
   the fourteen Functions in the Forward migration.
5. Therefore the two POS rows are the only Stable Contract delta: 37 − 35 = 2 rows and
   5498 − 5277 = 221 bytes, exactly the two synthetic lines plus their two line separators.
6. The final canonical strings contain no terminal newline.

## 6. Prompt-27 Correction Record

| Item | Rejected Prompt-26/27 candidate | Corrected value |
| --- | --- | --- |
| Function identity | `public.create_pos_sale(uuid,uuid,jsonb)` (hand-typed short form) | `public.create_pos_sale(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)` |
| Boolean rendering | `false` | `f` |
| Rows | 37 | 37 |
| Bytes | 5422 (approved) / 5506 (recomputed with `false`) | 5498 |
| MD5 | `bb59784870a414bfae5a18914453d5d4` / `fbe1be955a2df27a8025d4f5b732cbd6` | `36da554aef9a68d2acfbe9e1663c5def` |

- Signature delta: the short signature is 42 characters shorter per line; across the two
  synthetic rows this is an exact **84-byte** difference.
- Boolean delta: `false` is 4 characters longer than `f`; across the two synthetic rows this is
  an exact **8-byte** difference.
- The candidate `37 / 5506 / fbe1be955a2df27a8025d4f5b732cbd6` is **permanently rejected** for
  this contract and is recorded here only as a Prompt-27 defect.

## 7. Platform Sandbox Observation (non-blocking)

| Field | Value |
| --- | --- |
| Rows | 14 |
| UTF-8 bytes | 2449 |
| MD5 | `9fa9afcfb207e5911f8b17eafc8e9adb` |
| SHA-256 | `398631e3b7093b600e1faf37a1468b76b5522acee141659ec90cfb33a45fdd4b` |
| Evidence time | 04-08-2026 15:21 Asia/Riyadh (UTC+03:00) — 2026-08-04T12:21Z |
| Status | Non-blocking platform observation |

Observed roles matching `sandbox_exec*`: `sandbox_exec` (OID 127690) and
`sandbox_exec_vhxglsvxwwpmoqjabfmj` (OID 123688). Role OIDs are external metadata only and do
not appear inside the canonical string.

Per-role Function count over the fourteen Functions:

| Role | Rows |
| --- | ---: |
| `sandbox_exec_vhxglsvxwwpmoqjabfmj` | 14 |
| `sandbox_exec` | 0 |

Complete canonical string (all 14 lines, no ellipsis, no terminal newline):

```text
public._finance_invoice_approve_inline(p_tenant_id uuid, p_invoice_id uuid, p_actor uuid)|postgres|sandbox_exec_vhxglsvxwwpmoqjabfmj|EXECUTE|f
public._finance_ledger_insert(p_tenant_id uuid, p_client_id uuid, p_entry_type text, p_reference_type text, p_reference_id uuid, p_amount numeric, p_effective_date date, p_description text, p_payment_method text, p_payment_session_id uuid, p_metadata jsonb, p_created_by uuid)|postgres|sandbox_exec_vhxglsvxwwpmoqjabfmj|EXECUTE|f
public.approve_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid)|postgres|sandbox_exec_vhxglsvxwwpmoqjabfmj|EXECUTE|f
public.cancel_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid, p_effective_date date, p_reason text)|postgres|sandbox_exec_vhxglsvxwwpmoqjabfmj|EXECUTE|f
public.create_invoice_with_items(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)|postgres|sandbox_exec_vhxglsvxwwpmoqjabfmj|EXECUTE|f
public.create_pos_sale(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)|postgres|sandbox_exec_vhxglsvxwwpmoqjabfmj|EXECUTE|f
public.create_source_checkout_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)|postgres|sandbox_exec_vhxglsvxwwpmoqjabfmj|EXECUTE|f
public.delete_draft_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid)|postgres|sandbox_exec_vhxglsvxwwpmoqjabfmj|EXECUTE|f
public.post_expense_with_ledger(p_tenant_id uuid, p_idempotency_key uuid, p_expense_id uuid)|postgres|sandbox_exec_vhxglsvxwwpmoqjabfmj|EXECUTE|f
public.post_invoice_payments(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid, p_account_id uuid, p_payment_date date, p_payments jsonb)|postgres|sandbox_exec_vhxglsvxwwpmoqjabfmj|EXECUTE|f
public.post_manual_ledger_adjustment(p_tenant_id uuid, p_idempotency_key uuid, p_client_id uuid, p_amount numeric, p_effective_date date, p_description text)|postgres|sandbox_exec_vhxglsvxwwpmoqjabfmj|EXECUTE|f
public.post_payment(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid, p_amount numeric, p_payment_date date, p_payment_method text, p_account_id uuid, p_payload jsonb)|postgres|sandbox_exec_vhxglsvxwwpmoqjabfmj|EXECUTE|f
public.post_payment_session(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)|postgres|sandbox_exec_vhxglsvxwwpmoqjabfmj|EXECUTE|f
public.update_invoice_with_items(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid, p_payload jsonb)|postgres|sandbox_exec_vhxglsvxwwpmoqjabfmj|EXECUTE|f
```

Role/function matrix:

| # | Function identity | Grantor | Grantee | Privilege | Grantable |
| ---: | --- | --- | --- | --- | --- |
| 1 | `public._finance_invoice_approve_inline(p_tenant_id uuid, p_invoice_id uuid, p_actor uuid)` | postgres | sandbox_exec_vhxglsvxwwpmoqjabfmj | EXECUTE | f |
| 2 | `public._finance_ledger_insert(p_tenant_id uuid, p_client_id uuid, p_entry_type text, p_reference_type text, p_reference_id uuid, p_amount numeric, p_effective_date date, p_description text, p_payment_method text, p_payment_session_id uuid, p_metadata jsonb, p_created_by uuid)` | postgres | sandbox_exec_vhxglsvxwwpmoqjabfmj | EXECUTE | f |
| 3 | `public.approve_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid)` | postgres | sandbox_exec_vhxglsvxwwpmoqjabfmj | EXECUTE | f |
| 4 | `public.cancel_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid, p_effective_date date, p_reason text)` | postgres | sandbox_exec_vhxglsvxwwpmoqjabfmj | EXECUTE | f |
| 5 | `public.create_invoice_with_items(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)` | postgres | sandbox_exec_vhxglsvxwwpmoqjabfmj | EXECUTE | f |
| 6 | `public.create_pos_sale(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)` | postgres | sandbox_exec_vhxglsvxwwpmoqjabfmj | EXECUTE | f |
| 7 | `public.create_source_checkout_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)` | postgres | sandbox_exec_vhxglsvxwwpmoqjabfmj | EXECUTE | f |
| 8 | `public.delete_draft_invoice(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid)` | postgres | sandbox_exec_vhxglsvxwwpmoqjabfmj | EXECUTE | f |
| 9 | `public.post_expense_with_ledger(p_tenant_id uuid, p_idempotency_key uuid, p_expense_id uuid)` | postgres | sandbox_exec_vhxglsvxwwpmoqjabfmj | EXECUTE | f |
| 10 | `public.post_invoice_payments(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid, p_account_id uuid, p_payment_date date, p_payments jsonb)` | postgres | sandbox_exec_vhxglsvxwwpmoqjabfmj | EXECUTE | f |
| 11 | `public.post_manual_ledger_adjustment(p_tenant_id uuid, p_idempotency_key uuid, p_client_id uuid, p_amount numeric, p_effective_date date, p_description text)` | postgres | sandbox_exec_vhxglsvxwwpmoqjabfmj | EXECUTE | f |
| 12 | `public.post_payment(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid, p_amount numeric, p_payment_date date, p_payment_method text, p_account_id uuid, p_payload jsonb)` | postgres | sandbox_exec_vhxglsvxwwpmoqjabfmj | EXECUTE | f |
| 13 | `public.post_payment_session(p_tenant_id uuid, p_idempotency_key uuid, p_payload jsonb)` | postgres | sandbox_exec_vhxglsvxwwpmoqjabfmj | EXECUTE | f |
| 14 | `public.update_invoice_with_items(p_tenant_id uuid, p_idempotency_key uuid, p_invoice_id uuid, p_payload jsonb)` | postgres | sandbox_exec_vhxglsvxwwpmoqjabfmj | EXECUTE | f |

## 8. Historical Artifact Preservation

These values remain immutable, scope-bound historical or platform-inclusive observational
artifacts:

- State A:
  65 / b4138d2f6c8bf2ca01c41d437976d116

- Prompt-24 execution-point State B:
  63 / f2507d9a41a1bc76319b553328d8dd09

- Prompt-25/26 current all-role observation:
  49 / 3e807d782287d88bea69edeece0bb424

They are not Option-B Stable Security Acceptance fingerprints and must not be substituted for
the approved Option-B State-A or State-B artifacts.

## 9. Governed Preflight Evidence (read-only)

| Governed object | Expected | Observed |
| --- | --- | --- |
| Policy fingerprint | 3 / `04297828f4bd33eba043f6c9274ec57b` | 3 / `04297828f4bd33eba043f6c9274ec57b` |
| Table ACL fingerprint | 44 / `204017a1207bc68a246c3415e3975478` | 44 / `204017a1207bc68a246c3415e3975478` |
| `public.ledger_entries` | 88 / `23e73fd58f9308913ac978acee94b2f2` | 88 / `23e73fd58f9308913ac978acee94b2f2` |
| `public.customer_balances` | 8 / `22e38d161b126cca31f4c26830084012` | 8 / `22e38d161b126cca31f4c26830084012` |
| Helpers at `search_path=public, pg_temp` | 3 | 3 |
| `create_pos_sale` EXECUTE for anon / authenticated | false / false | false / false |
| Column ACL on governed tables | 0 | 0 |
| Browser-role inheritance | 0 | 0 |
| Owner postgres, RLS true, FORCE RLS false | 2 tables | 2 tables |
| Trusted schema (`CREATE` on `public` for browser roles) | false | false |
| Migrations after 20260804083738 | 0 | 0 |

## 10. Acceptance Boundary

- Persistence of this contract is **not** Acceptance.
- Prompt-25 Database QA **remains FAILED** until targeted independent QA of this artifact package.
- The Emergency Rollback remains **unexecuted**.
- The Stage-B Acceptance Re-Audit has **not** started.
- No Workstream, Phase or Roadmap Closure occurred.

## 11. Artifact Identity

Byte length, line count, SHA-256, Git blob ID and final-newline state for this file and for the
reissued `rollback.sql` are reported in the Prompt-28 execution report (Run Metadata), computed
after persistence.
