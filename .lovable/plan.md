# Prompt 52 — Correction Continuation A1 — RM-DH-004 Phase 2 Investigative Report

Read-only. Zero repository writes, zero migrations, zero database writes, zero storage writes, zero governance writes, zero Master Register writes.

## A. Primary Verdict

PROMPT-52 CORRECTION INVESTIGATION COMPLETE — READY FOR ONE BOUNDED NEXT ACTION

## B. All Required Verdicts

| Category | Verdict |
|---|---|
| Prompt identity | PROMPT 52 CORRECTION CONTINUATION A1 RUN — NO NEW NUMBER CONSUMED |
| Scope | PROMPT-52 CORRECTION SCOPE PRESERVED |
| Contamination | NO CROSS-TASK CONTAMINATION DETECTED |
| Drift | NO UNATTRIBUTED IN-SCOPE DRIFT |
| Prior Phase | PHASE 1 AND WS-DH-2026-0003 REMAIN OWNER-ACCEPTED AND CLOSED |
| Prior-report correction | PRIOR PROMPT-52 REPORT DEFECTS ACKNOWLEDGED AND CORRECTED |
| Governance | PHASE-2 BOUNDED GOVERNANCE RECONCILIATION CONTRACT COMPLETE |
| File Manager | EXISTING FILE MANAGER REQUIRES BOUNDED REFACTOR BEFORE SHARED REGISTRY USE |
| Upload flows | UPLOAD FLOWS ARE FRAGMENTED — BOUNDED CONSOLIDATION SEAM REQUIRED |
| Connections | EXISTING CONNECTIONS ARCHITECTURE PROVIDES A REUSABLE NATIVE-RECORD EXCHANGE FOUNDATION |
| EQX | NOT PRESENT IN THE REPOSITORY — NO SYSTEM, MODULE, TABLE, ROUTE OR IDENTIFIER NAMED `EQX` EXISTS (zero matches, whole-repo case-insensitive search) |
| Import substrate | PARTIAL REUSABLE FILE SUBSTRATE EXISTS — EXACT REUSE BOUNDARY PROVEN |
| Ecosystem separation | PLATFORM FILE AND RECORD RESPONSIBILITIES REMAIN CONFLATED (unregistered uploads and unindexed storage objects) |
| Shared Foundation | PLATFORM-WIDE SHARED CORE AND DOMAIN ADAPTER CONTRACT DEFINED |
| Finance delivery | FINANCIAL HISTORICAL IMPORT CLIENT-DELIVERY PATH DEFINED |
| Classification | ALL DISCOVERIES CLASSIFIED WITHOUT PHASE INFLATION |
| Execution | ONE BOUNDED NEXT-PROMPT CONTRACT DEFINED |

## C. Complete Current Roadmap

- Mission: Financial Truth Stabilization and Historical Data Migration (RM-DH-004, P0).
- Phase 0 — Governance Persistence: COMPLETED, Owner-advanced.
- Phase 1 — Economic Date Integrity (WS-DH-2026-0003): Stages A, B accepted and persisted; Stage C (read-path cutover) and Stage D (`ledger_entries.effective_date NOT NULL`, migration `20260805122454_…`) executed and QA-passed in the Prompt 44–51 lineage.
- Owner-approved Phase 2: Shared Platform-Wide Historical Import Foundation — Finance-first, Workstream WS-DH-2026-0006.
- Persisted stale mapping (`roadmap.md` v1.2.0, lines 236–252, 296–301): Phase 2 = "Canonical Financial Write Authority" (WS-0004, WS-0005); Phase 3 = "Import Control Plane" (WS-0006). This contradicts the Owner-approved direction.
- Second persisted staleness: the Status block still records "Stage C and Stage D: NOT STARTED" although both executed.
- Recommended corrected mapping: Phase 2 → Shared Platform-Wide Historical Import Foundation (WS-DH-2026-0006, Finance-first Adapter); the Canonical Financial Write Authority content is re-sequenced per the WS-0004 disposition (section J) without renumbering any Phase already closed.
- Phases 4–8 and WS-0007 … WS-0011 unchanged. No silent renumbering performed or proposed.

## D. Owner Decisions Already Closed

RM-DH-004 governs; no new Roadmap; Phase 1 closed; Phase 2 current; WS-DH-2026-0006 is the Workstream; platform-wide Shared Core with Finance as first Domain Adapter; PDF + Excel first, images architecturally supported; no Phase inflation; Master Register untouched. None of these is reopened.

## E. Active Parallel Task Ledger

- PT-DH-RM004-WS0006-P52A1-CORRECTION-20260805-2135 — this run (read-only).
- PT-DH-WS0012-MEMORY-GENESIS-INVESTIGATION-01 — reserved, not started (RM-DH-003 lane).
- PT-DH-WS0013-REFACTORING-BASELINE-INVESTIGATION-01 — reserved, not started (RM-DH-003 lane).

## F. CROSS-TASK REPORT CONTAMINATION CHECK

NO CROSS-TASK CONTAMINATION DETECTED

Post-Prompt-51 commits are alternating platform pairs (`Changes` / `Update plan`): `87ff9d66`, `3871c033`, `50a802a1`, `c762168c`, `a45d647c`, `31348509`, `c22b9210`, `840865bc`, `cd1bd83c`, `501f24a1`, `37bf4049`, `a275c754`. Their content changes are confined to `.lovable/plan.md` (platform-managed artifact, DEC-RM-DH-003-004 exception) plus the RM-DH-003 governance paths written under Prompt 16. No RM-DH-004 governed path, no migration, no application file and no storage object changed in this window. Text contamination: none. Repository drift: attributed. Database drift: none observed (read-only). Governance drift: the two stale RM-DH-004 statements in section C are pre-existing, not drift from this window.

## G. Branch, Full HEAD, Parents, Working Tree and Drift

- Branch: `edit/edt-50b63ead-b10f-4561-8073-dca10066858f`
- HEAD: `a275c754feb653d35524c930497508d7f5a37641` (merge)
- Parents: `501f24a1f12bb131ca7a1f3006467e7a44d3d833`, `37bf40499aef9b9f91ae08f3f6c51ee1ea835b15`
- Working Tree before and after investigation: clean, zero changed paths.
- Migration history: 325 files; newest `20260805122454_6b75ebd2-…` (Stage-D NOT NULL). Prompt-49 ancestry intact.
- `.lovable/plan.md` disclosed: it is the report artifact for this run.

## H. Prior Prompt-52 Report Correction Ledger

| # | Prior claim | Error | Impact | Action | Replacement | Evidence |
|---|---|---|---|---|---|---|
| 1 | "NO IMPORT SUBSTRATE EXISTS" | Absolute, contradicted by the report's own findings | Risk of duplicate file infrastructure | WITHDRAWN | PARTIAL REUSABLE FILE SUBSTRATE EXISTS — EXACT REUSE BOUNDARY PROVEN | `media_assets` (2 rows), `media_asset_clients` (0), `media_share_links` (0), private `horse-media` bucket, live File Manager route |
| 2 | Proposed model labelled "Option B" | Described a hybrid core, not typed Domain tables | Wrong architecture record | LABEL WITHDRAWN | Correct label is Option C — Hybrid Shared Core with typed/projected Domain fields (section AI) | Prompt-52 option contract |
| 3 | Recommended repositioning WS-DH-2026-0004 | Residual scope never proven | Could delete real remaining work | WITHDRAWN | Evidence-backed disposition in section J | Stage-B artifacts, live grants |
| 4 | Reopened Phase remap and WS-0006 use as Owner questions | Already Owner-decided | Delay | WITHDRAWN | Treated as controlling decisions | Section D |
| 5 | Contamination verdict omitted | Mandatory section missing | Report not acceptable | CORRECTED | Section F | This run |
| 6 | Abbreviated HEAD, missing metadata | Non-reproducible evidence | Weak audit trail | CORRECTED | Sections G and BH | This run |
| 7 | Major report sections omitted | Incomplete contract | Non-decision-complete | CORRECTED | Sections M–AT | This run |

## I. Current Governance State

RM-DH-004 package present (`README.md`, `roadmap.md`, `decisions.md`, `changelog.md`), roadmap v1.2.0. `docs/workstreams/` contains packages only for WS-DH-2026-0003, 0012, 0013 — **WS-DH-2026-0006 has no package** (registry cell "Not yet created" is accurate). Registry marks WS-0006 "BLOCKED BY PHASES 1–2" under the stale Phase-3 mapping.

## J. WS-DH-2026-0004 Evidence Matrix

Registered scope: Canonical Financial Write Authority (Phase 2, blocked by WS-0003, no package). Compared item-by-item with Stage-B accepted work: browser-role direct DML on `invoices`, `invoice_items`, `ledger_entries`, `payments` was revoked; expense approval was cut over to `post_expense_with_ledger`; canonical RPCs (`create_source_checkout_invoice`, `post_payment_session`, `_finance_invoice_approve_inline`) are the write authority; helper functions hardened with empty `search_path`.

Disposition: **PARTIALLY COMPLETED WITH BOUNDED RESIDUAL SCOPE.** Residual items evidenced: residual Finance privilege hardening, full `has_permission` qualification, duplicate Ledger SELECT policy, TEMP grant / PUBLIC helper EXECUTE review, Manual Ledger Adjustment UI, Supplier Payable lifecycle authority. These are not blockers for Historical Import posting, because posting will route through the same canonical RPC authority already accepted in Stage B.

## K. WS-DH-2026-0005 State

POS remains Coming Soon and operationally inert: `src/pages/finance/POSComingSoon.tsx`, `pos_sales` table exists with **0 rows**, no `create_pos_sale`. Deferred and isolated; does not affect Phase-2 sequencing.

## L. WS-DH-2026-0006 Identity and Package Contract

- Title: Shared Platform-Wide Historical Import Foundation — Finance-First Adapter.
- Purpose: shared external-file registry seam, import control plane (batch, staging, validation, quarantine, dry run, reconciliation, approval), and the Finance Domain Adapter posting through existing canonical RPCs.
- Phase: RM-DH-004 / Phase 2 (after reconciliation).
- Package: `README.md`, `roadmap.md`, `decisions.md`, `changelog.md` at 1.0.0 under `docs/workstreams/ws-dh-2026-0006-shared-platform-historical-import-foundation/`.
- Initial decisions: staging Option C; processing Option D; storage Option C (dedicated private import bucket + File Manager as unified view); permission namespace `import.*`.
- Dependencies: Phase 1 (closed). Not blocked by WS-0004 residuals.
- Status after creation: ACTIVE — PACKAGE CREATED — INVESTIGATION/IMPLEMENTATION NOT YET RUN.

## M. File Manager UI Audit

`src/pages/DashboardFileManager.tsx` (517 lines), routed in `App.tsx`, reachable from `DashboardRecords.tsx`. Live but thin: statistics (total files, total size, images, videos) computed client-side from the fetched rows; Grid/List toggle; search; visibility badges (private / tenant / shared_link); selection checkboxes; view and delete actions; RTL-aware classes (`start-2`); i18n under `files.*`.

Confirmed defects:
- **Previews are placeholders.** `FileCard` renders a lucide icon for image, video and other MIME types; no signed URL is ever requested. This fully explains the blank previews in the Owner evidence — nothing is broken at the storage layer, the thumbnail was never implemented.
- No PDF / Excel / CSV category; the type taxonomy is image / video / other.
- No upload action on the page — files can only arrive from Domain flows.
- Delete is a hard delete (storage `remove` + row `delete`), with no archive, no evidence protection and no confirmation of downstream links.
- Domain-link rendering is absent; `entity_type` / `entity_id` are not resolved to Horse / Invoice / Expense / Lab labels.
- Statistics are truthful for fetched rows only, and rows are the wrong denominator (section O).
- `حصان` appears 39 times in `src/i18n/locales/ar.ts` and in 6 other source files. It is live source text, not only old UI evidence. The `خيل` correction is a bounded, mechanical, low-risk candidate — but it touches shared translation keys beyond the File Manager, so it is scoped as Category B only for the File Manager surface.

## N. File Manager Repository Inventory

`src/pages/DashboardFileManager.tsx`; `src/hooks/useMediaAssets.ts` (111 lines); `src/hooks/useMediaShareLinks.ts`; `src/i18n/locales/{ar,en}.ts` (`files.*`); `supabase/functions/shared-media-sign/index.ts`. `useMediaAssets` queries with `.from("media_assets" as any)` and a stale comment claiming the table "needs to be created" — the table exists; the cast means the generated types are not used and the query is untyped. Filtering by `entity_type`/`entity_id` is applied client-side after fetching the whole tenant set: a correctness and scale defect.

## O. Media and File Database Inventory

- `media_assets`: 2 rows, all `entity_type = 'expense'`. Columns include tenant, entity_type/entity_id (polymorphic), bucket, path, filename, mime_type, size_bytes, visibility, created_by, display_order, alt_text.
- `media_asset_clients`: 0 rows. `media_share_links`: 0 rows.
- `storage.objects`: **7 objects** versus 2 registry rows — at least 5 stored objects are unindexed by the registry. Not every uploaded file receives an asset record.
- No `import*`, `staging*`, `quarantine*`, `attachment*` or `document*` import tables exist. File-like public tables: `media_assets`, `media_asset_clients`, `media_share_links`, `contract_documents`, `contract_document_events`.
- Missing for import use: origin/source classification, checksum, processing state, version lineage, retention class, archive flag.

## P. Storage and RLS Audit

Buckets: `horse-media` (private, 52,428,800 byte limit, MIME allowlist = jpeg/png/webp/gif, mp4/webm/quicktime) and `database_export_20_07_26` (private, no limit, no allowlist).

**The allowlist blocks PDF, Excel and CSV outright.** `storage.objects` policies are all predicated on `bucket_id = 'horse-media'`, so any new bucket starts with zero policies (fail-closed, correct default but requires explicit policies).

`media_assets` RLS: SELECT `is_tenant_member(...) AND (visibility IN ('tenant','shared_link') OR created_by = auth.uid())`; INSERT/UPDATE/DELETE gated on `has_permission(..., 'files.assets.manage')`. `media_share_links` and `media_asset_clients` mix legacy `has_tenant_role('owner'/'manager')` with `has_permission` — inconsistent with the Core rule that role checks are replaced by granular permissions. Tenant isolation is present on all three tables. Share links have no evidenced expiry/revocation audit in use (0 rows).

## Q. Upload-Flow Matrix

| Flow | Path | Bucket | Registry row | Appears in File Manager | Reusable for import |
|---|---|---|---|---|---|
| Horse media | `src/components/horses/wizard/StepMedia.tsx` | `horse-media` | inserts `media_assets` | Yes | Pattern only |
| Horse wizard finalize | `src/components/horses/HorseWizard.tsx:565` | `horse-media` | inserts `media_assets` | Yes | Pattern only |
| Expense attachment | `src/components/finance/ExpenseFormDialog.tsx:112` | `horse-media` | inserts `media_assets` | Yes | Closest analogue |
| Everything else (lab, vet, contracts, services, supplier docs) | — | — | — | No such flow exists | n/a |

`supabase.storage.from(...)` appears at only three call sites, all `horse-media` and all in `StepMedia.tsx`. There is **no PDF/Excel/CSV upload path anywhere in the codebase**, and no upload edge function. Verdict: UPLOAD FLOWS ARE FRAGMENTED — BOUNDED CONSOLIDATION SEAM REQUIRED.

## R. File Manager Scope Contract

The Owner contract (include external binaries; exclude native records and regenerable exports; full linkage metadata; no physical duplication; truthful Historical-Import status) is **assessed as sound and adoptable**. Current implementation satisfies only: tenant, uploader, linked object (weakly), filename, MIME, size, upload time. Missing: origin, source module, file category, access contract, processing state, archive state, checksum.

## S. File Manager Reuse Options

| | A — Reuse as-is | B — Refactor into shared registry | C — Separate import storage + registry, File Manager as view |
|---|---|---|---|
| Time | Lowest | Medium | Low-medium |
| Risk | High (MIME allowlist blocks PDF/Excel; hard delete destroys evidence) | Medium (touches live horse/expense flows) | Low (new isolated bucket + table; no live flow touched) |
| Duplicate infra | None | None | One extra bucket, one extra registry table |
| Data migration | None | Backfill origin/state on 2 rows | None |
| RLS complexity | n/a | Rework 3 tables' policies | New policies only, fail-closed |
| Client-delivery impact | Blocked | Delays pilot | Fastest safe |
| Future Domain fit | Poor | Good | Good (registry union view) |
| Rollback | n/a | Harder (live tables) | Drop new objects |

**Recommended: Option C**, with the File Manager becoming a presentation layer over both registries and a Category-B fix to its previews and taxonomy. Option B is the correct follow-on once the pilot is delivered.

## T. Recommended Shared External File Registry Architecture

Dedicated private bucket `historical-import` (PDF, Excel, CSV, images; explicit size limit; tenant-prefixed paths; signed URLs only) plus an import file registry carrying tenant, uploader, origin, domain, checksum (SHA-256), MIME, size, processing state, retention class and archive flag. The File Manager reads a union view over `media_assets` and the import registry. One physical object, many references; no binary duplication.

## U. External Files versus Native Records Matrix

| Concept | Storage | Registry | Current state |
|---|---|---|---|
| External upload | bucket object | should be registry row | Partially registered (7 objects / 2 rows) |
| Native record (invoice, lab result, statement) | none | Domain table | Correct — not duplicated into files |
| Generated PDF / print / CSV export | none (on demand) | none | Correct — no permanent exports created |
| Connection-delivered record | none | share/grant tables | Correct — native, not file copy |
| Import source file | bucket object | import registry (absent) | **Gap — Category A** |

## V. Connections Architecture Audit

Live tables: `connections` (18 rows; statuses pending / accepted / revoked), `consent_grants` (5), `connection_horse_access` (2), `horse_shares` (1), `horse_share_packs`, `lab_report_shares` (2), `lab_report_share_results`, `lab_result_shares`, `connection_messages`, `connection_rate_limits`, `media_share_links` (0). Hooks: `useConnections`, `useConnectionsWithDetails`, `useConsentGrants`, `useSharingAuditLog`, `useLabReportShares`. Edge function `expire-stale-connections` handles lifecycle expiry; `shared-media-sign` signs shared media.

Findings: Laboratory report/result delivery is the one proven end-to-end native-record exchange (sender approval → share row → recipient surface → notification, with a sharing audit log). Horse access sharing is partial. Finance (invoices, statements) has **no** connection-delivered path. Recipients see shared native records, not copies. Revocation is modelled (`revoked` status, grant rows). Cross-tenant isolation is enforced through connection/grant predicates.

## W. Connections Integration Seam

Phase 2 creates no Connections system. The only seam required now: imported native records must carry normal Domain identity and tenant ownership so existing `consent_grants` / share mechanisms apply unchanged later. No new sharing table, no Connection expansion.

## X. EQX Audit

Whole-repository case-insensitive search for `eqx` returns **zero matches** — no route, page, component, hook, table, function, migration, translation key or comment. There is no EQX system in this codebase. If EQX exists, it is external to the repository and outside this evidence base; the Owner must supply a direct source before it can be audited.

## Y. Current Historical Import Substrate Verdict

PARTIAL REUSABLE FILE SUBSTRATE EXISTS — EXACT REUSE BOUNDARY PROVEN. Reusable: registry table shape, tenant-scoped RLS pattern, permission gating, private-bucket + signed-URL pattern, File Manager shell. Not reusable: `horse-media` MIME allowlist, hard-delete semantics, absent processing/origin metadata, absent staging/quarantine/batch objects, absent PDF/Excel parsing, absent durable queue.

## Z–AB. Shared Import Core, Domain Adapters, Finance-First Adapter

Shared Core (domain-agnostic): import batch, source file registry, staging rows with JSONB payload + projected typed columns, validation issues, quarantine, dry-run result, reconciliation summary, approval and audit. Domain Adapter interface: `validate(batch)`, `dryRun(batch)`, `post(batch)` — each adapter owns its typed projection and posting authority. Finance Adapter: maps staged rows to clients, invoices, payments and opening obligations, posts exclusively through the existing canonical RPCs so Stage-B write authority and `effective_date NOT NULL` remain the single truth.

## AC–AE. PDF, Excel and Image readiness

PDF levels supported today: **level 1 only (source evidence storage)** — and even that is blocked by the bucket allowlist. Levels 2–6 (digital text, tables, multi-document segmentation, OCR, low-confidence review) are unimplemented; no PDF library exists in the codebase. Excel: no `.xlsx`/`.xls` reader present; multi-sheet, dates, amounts, formulas, hidden rows, bilingual headers and source-cell traceability all unimplemented. Images: storage-supported, OCR absent — architectural seam only (`ocr_provider`, `confidence`, `requires_review`). The pilot must declare PDF as evidence-preservation plus digital-text/table extraction, and Excel as the primary structured path.

## AF–AG. Storage, Quota, Deletion and Evidence

No per-tenant storage measurement exists. `size_bytes` is present on `media_assets` and is the sufficient seam; add tenant attribution and an original/temporary/evidence classification. No pricing plans in Phase 2. Evidence files must be delete-blocked while downstream native records exist (archive only, replacement creates a new version); ordinary external files may use archive/trash; temporary artifacts get bounded retention. The current hard delete in `useMediaAssets` must never be applied to import source files.

## AH–AI. Staging Options and Recommendation

Option A (JSONB only) — fast, but weak Finance precision, poor indexing and validation. Option B (fully typed per Domain) — best queryability, worst schema sprawl and slowest to deliver, blocks future Adapters. **Option C (Hybrid Shared Core with typed/projected Domain fields) — RECOMMENDED.** It preserves raw source fidelity (row/cell traceability in JSONB), gives Finance the typed, indexed, exact-numeric columns reconciliation requires, keeps one RLS surface, and admits future Adapters without new core tables. The prior report's "Option B" label is withdrawn; the described model was Option C.

## AJ–AK. Processing Options and Recommendation

No durable queue exists in this project — stated plainly, none invented. Available: browser, Edge Functions (short-lived, per Supabase Functions documentation), and Postgres. Option A (browser-only) is not evidence-reproducible. Option B (synchronous Edge) breaks on large files/timeouts. Option C (async worker) requires infrastructure that does not exist. **Option D (hybrid: client-side preview/parse of the selected file, authoritative server-side validation, chunked staging inserts, and a DB-backed batch state machine polled by the UI) — RECOMMENDED.** Durable state lives in the batch/staging tables, giving retry, cancellation, idempotency and progress without inventing a queue. OCR is deferred behind a provider interface.

## AL–AM. Permission Vocabulary and RLS Authority

Existing vocabulary contains `files.assets.manage` and the `finance.*` family; **zero `import.*` keys exist**. Repository convention is dotted, lower-camel leaf segments. Recommended namespace: **`import.*`** (platform-wide), not `finance.import.*`, so future Laboratory/Veterinary Adapters are not governed by a Finance-only key; per-domain scoping is expressed by the batch's domain column plus the Domain permission required at posting time (`finance.invoice.create` etc.). Stage separation must be enforceable: upload, processing, review, approve and post-authorize are distinct keys; one person may hold all of them, but each transition is separately audited. All new tables tenant-scoped, RLS enabled, explicit GRANTs, posting only through SECURITY DEFINER RPCs.

## AN. Security and Negative-Test Matrix

Cross-tenant staging read denied; upload to another tenant's path denied; approval without `import.approve` denied; posting without Domain permission denied; dry run writes zero target rows; duplicate batch (same checksum) rejected; partial failure leaves zero posted rows; source file undeletable while posted records exist; signed URL expiry enforced; new bucket has no public access.

## AO. Small Co-Traveller Fix Matrix (Category B candidates)

1. File Manager image previews via signed URLs — assess, low risk.
2. PDF/Excel/CSV category + icons in the File Manager taxonomy — low risk.
3. `حصان` → `خيل` on File Manager surfaces only — low risk, mechanical.
4. Remove the false "table does not exist" comment and the `as any` cast in `useMediaAssets`; move entity filtering into the query — low risk, correctness fix.
5. Align `media_share_links` / `media_asset_clients` policies onto `has_permission` — small, but touches RLS; assess before bundling.

## AP. Required-Now versus Future Classification

- **Category A (required now):** import batch / source file registry / staging / validation / quarantine / dry-run / reconciliation / approval tables; dedicated private import bucket with PDF+Excel+CSV+image MIME set and policies; Excel parsing; PDF text+table extraction; `import.*` permissions; Finance Adapter posting via canonical RPCs; source traceability and checksum; audit trail.
- **Category B:** the five co-traveller fixes above.
- **Category C (seam now, capability later):** Domain Adapter interface; Connections integration seam; tenant storage attribution seam; OCR provider interface; version lineage; retention class.
- **Category D (deferred):** full File Manager redesign; all future Domain Adapters; storage plans/add-ons; Connections expansion; supplier and Pharmacy exchange; duplicate cleanup; cold storage; WS-0004 residual hardening items.
- **Category E (out of scope):** RM-DH-003 Phase 3 knowledge work; POS implementation.

## AQ–AR. Critical Path and Execution Slices

Governance reconciliation + WS-0006 package → Slice 1 control-plane schema, bucket and permissions → Slice 2 upload + File Manager visibility of import files → Slice 3 Excel ingestion, staging and validation → Slice 4 quarantine, review and correction → Slice 5 dry run and reconciliation to 0.00 → Slice 6 approval and canonical posting with idempotency → Slice 7 PDF text/table extraction → Slice 8 controlled pilot with the waiting client. The client can safely use the feature at the end of Slice 6 for Excel sources, and after Slice 7 for PDF sources.

## AS. Exact Next-Prompt Contract

**Option 2 — Agent/Build: bounded Governance Reconciliation plus minimal Shared Control Foundation.**

- Mode: Agent/Build.
- Objective: correct the RM-DH-004 Phase 2/3 mapping and the stale Stage C/D status; create the WS-DH-2026-0006 four-file package at 1.0.0; create the minimal control-plane schema and the private import bucket.
- Authorized files: `docs/roadmaps/rm-dh-004-…/{roadmap.md,decisions.md,changelog.md}`, `docs/README.md`, `docs/workstreams/README.md`, the four new WS-0006 package files, one new migration.
- Authorized DB objects: new import batch/file/staging/issue/quarantine tables with GRANTs, RLS and policies; new `import.*` permission keys.
- Authorized storage: one new private bucket with explicit MIME allowlist and size limit.
- Exclusions: no parsing implementation, no posting RPC, no File Manager refactor, no Connections change, no Master Register write, no Finance data write.
- Rollback: exact-file restoration plus a drop-only migration for the new objects.
- Tests: RLS negative tests per section AN (cross-tenant read/write, permission-less approval).
- Acceptance: schema and bucket exist with proven fail-closed policies; zero change to existing Finance data; Phase-1 contracts intact.
- Evidence: branch, full HEAD, migration SHA, policy dump, bucket configuration.
- Stopping point: immediately after the migration, bucket and governance writes — no parsing, no UI.

## AT. Phase-2 Acceptance Contract

Shared Foundation Acceptance: control-plane schema, bucket, permissions, RLS negative tests, rollback proven. Finance Adapter Acceptance: the 24 listed conditions, of which the load-bearing ones are source preservation, tenant isolation, truthful format declaration, source traceability, quarantine with no best-guess matching, dry run with zero target writes, 0.00 unexplained reconciliation difference, canonical posting authority, real Economic Dates, idempotency, no partial posting, full audit, zero Phase-1 regression. First Real-Client Pilot Acceptance: controlled pilot evidence plus explicit Owner Acceptance before production use. Future PDF/OCR maturity and future Domain Adapters are explicitly **not** required to close the Finance-first milestone.

## AU. Owner Decisions Required

None.

## AV–BA. Facts, Owner Decisions, Lovable Claims, Inferences, Gaps, Contradictions

- Facts: all counts, blobs, policies, buckets and file paths cited above are direct reads of the live repository and database.
- Owner decisions: section D.
- Lovable claims: none accepted without repository or database confirmation.
- Inferences (labelled): blank previews are caused by the placeholder-icon renderer; the 5-object/2-row delta implies unregistered uploads.
- Gaps: no direct evidence of EQX; no per-tenant storage measurement; no parsing capability; exact Owner-side timestamps not recorded.
- Contradictions: persisted Phase 2/3 mapping versus Owner-approved direction; roadmap "Stage C and Stage D: NOT STARTED" versus executed Stage C/D.

## BB. Blocking Findings

None. The two governance contradictions are correctable inside the recommended next action.

## BC. Non-Blocking Residuals

Legacy role checks in two media policies; stale comment and untyped cast in `useMediaAssets`; client-side entity filtering; `database_export_20_07_26` bucket has no size or MIME limits.

## BD. One Recommendation

Execute the Section AS Option-2 contract: bounded Governance Reconciliation plus the minimal Shared Control Foundation.

## BE. Complete Deferred Items Register

All 45 preserved items remain registered, bilingual, unchanged in wording. Classification applied:

- **PROMOTED TO CURRENT INVESTIGATIVE SCOPE / نُقل إلى النطاق الاستقصائي الحالي:** 25 — Shared Platform-Wide Historical Import Foundation / البنية المشتركة للاستيراد التاريخي; 38 — File Manager and Media Assets audit / تدقيق مدير الملفات وأصول الوسائط.
- **Current (Category A):** 19 Historical Import provenance objects / كائنات مصدر الاستيراد التاريخي; 26 provenance, fingerprint, idempotency, quarantine, selective rollback / الأثر والبصمة والحجر والتراجع الانتقائي; 27 Opening Obligation / الالتزام الافتتاحي; 28 Unapplied Credit / الرصيد غير المخصص; 29 historical document identity and numbering / هوية وترقيم المستندات التاريخية; 30 Client, Payer, Horse Owner and Horse matching / مطابقة العميل والدافع والمالك والخيل.
- **Seam (Category C):** 20 `owner_id` identity binding / ربط هوية المالك; 32–35 future Domain Adapters / محولات المجالات المستقبلية; 40 Public/Private/Shared-Link access contract / عقد الوصول العام والخاص والرابط المشترك; 41 tenant storage measurement / قياس تخزين المستأجر; 42 Connected Domain Record Exchange expansion / توسعة تبادل السجلات; 44 temporary export retention / الاحتفاظ بالتصديرات المؤقتة; 45 evidence-file retention and deletion / الاحتفاظ بملفات الأدلة وحذفها.
- **Deferred (Category D):** 1–18, 21–24, 31, 36, 37, 39, 43 — including the stale Arabic Invoice PDF root assertion / تأكيد جذر فاتورة PDF العربية, HR salary atomicity / ذرّية الرواتب, POS implementation / تنفيذ نقاط البيع, Doctor billing unification / توحيد فوترة الطبيب, the Privileged Rollback-Only Writer drill / تمرين الكاتب المتميز, and File Manager UI/taxonomy/`خيل` remediation / معالجة واجهة مدير الملفات والمصطلحات.
- **Out of scope (Category E):** none newly added.

Master Register writes: ZERO.

## BF. WORKSTREAM PERSISTENCE

No persistence occurred. Recommended next persistence boundary: the Section AS Option-2 execution. WS-DH-2026-0003 remains closed and Owner-accepted. WS-DH-2026-0006 remains unmodified and packageless during this run.

## BG. ROADMAP IMPACT

RM-DH-004 remains ACTIVE. Phase 1 remains closed. Phase 2 remains current by Owner decision. No implementation, no automatic advancement, no Closure.

## BH. RUN METADATA AND EXACT STOPPING POINT

- Mode: Plan/Chat — read-only correction continuation. Parent Prompt 52 (consumed); continuation A1; no new number consumed; Prompt 53 remains next eligible.
- Parallel Task ID: PT-DH-RM004-WS0006-P52A1-CORRECTION-20260805-2135.
- Branch: `edit/edt-50b63ead-b10f-4561-8073-dca10066858f`. HEAD: `a275c754feb653d35524c930497508d7f5a37641`. Parents: `501f24a1…`, `37bf4049…`. Working Tree clean before and after; zero changed paths.
- Database boundary: read-only queries against `public` and `storage` catalogs and counts. Storage boundary: metadata reads only; no object read, written or deleted.
- Repository writes: 0. Migrations: 0. Governance writes: 0. Master Register writes: 0. Finance data writes: 0.
- Exact times for Owner-side events: Exact time not recorded.
- Contamination verdict: NO CROSS-TASK CONTAMINATION DETECTED.
- Stopping point: stopped immediately after completing the investigation and this report. No governance correction, no package creation, no schema or bucket creation.
- Next action: the Section AS Option-2 bounded execution, on Owner authorization.
