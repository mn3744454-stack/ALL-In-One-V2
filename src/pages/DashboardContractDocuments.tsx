// B2.5e — Route wrapper: renders the unified Contracts Hub shell.
// The Hub itself detects the pathname and mounts ContractDocumentsSection.
import DashboardContracts from "./DashboardContracts";

export default function DashboardContractDocuments() {
  return <DashboardContracts />;
}
